package com.osm.chat_app.chat;

public enum MessageType {
    // when a user send a message
    CHAT,
    // when a user join the message group
    JOIN,
    // when a user leaves the message group
    LEAVE
}
